import React from "react";
import { container, box } from "./index.module.css";

export default function index() {
  return (
    <div>
      index
      <div className={`${container} ${box}`}>asd</div>
    </div>
  );
}
